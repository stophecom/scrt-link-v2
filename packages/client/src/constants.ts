export const MASTER_PASSWORD_LENGTH = 36;
// We use a substring of the master password to generate a secret id.
// SECRET_ID_LENGTH should be smaller than MASTER_PASSWORD_LENGTH
export const SECRET_ID_LENGTH = 28;

export const TRIAL_PERIOD_DAYS = 7;
